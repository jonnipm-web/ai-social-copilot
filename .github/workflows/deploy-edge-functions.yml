name: Deploy Edge Functions

on:
  push:
    branches: [main]
    paths:
      - 'supabase/functions/**'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Deploy all Edge Functions
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: |
          # process-file is ~33 MB and exceeds Supabase's upload limit — skip it
          SKIP="process-file"
          for fn in supabase/functions/*/; do
            name=$(basename "$fn")
            if [[ "$name" == "$SKIP" ]]; then
              echo "⚠ Skipping $name (exceeds size limit)"
              continue
            fi
            echo "→ Deploying $name"
            supabase functions deploy "$name" \
              --project-ref nzngvbajrnruknpzzjbf \
              --no-verify-jwt
          done
