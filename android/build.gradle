// Root build.gradle — configuração de nível de projeto.
// O Flutter Gradle plugin gerencia a maior parte das dependências.

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

tasks.register("clean", Delete) {
    delete rootProject.layout.buildDirectory
}
